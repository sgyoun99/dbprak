package main;

/**
 * enum for Exception classification
 *
 */
public enum ErrType {
	XML, XML_NO_VALUE, XML_INVALID_VALUE, XML_DATA_INCOMPLETE, XML_NO_ATTRIBUTE, XML_NO_NODE, SQL, SQL_FK_ERROR, SQL_DUPLICATE, PROGRAM;
}
